apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: digital-library-media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
